
notes


jsconst CAROUSEL = {
  dir: 'photos/',          // folder containing your images
  images: [
    { file: 'image1.jpg', caption: 'Research & fieldwork' },
    { file: 'image2.jpg', caption: 'Speaking & outreach'  },
    // just add more lines here
  ]
};
```

Your repo structure would look like:
```
index.html
index.css
photos/
  image1.jpg
  image2.jpg
  image3.jpg


Here's the approach I'll take — a JavaScript-driven carousel that scans a configurable list of images from a directory, so you just drop files in and update one array at the top of the script.
Since GitHub Pages is purely static (no server-side directory listing), the cleanest solution is a small config array at the top of the JS — you set the folder path and filenames once, and the carousel builds itself automatically.Viewed a file, edited a fileViewed a file, edited a fileNow replace the old script block with the new self-building carousel:Viewed 2 files, edited a fileViewed 2 files, edited a fileThe file is being truncated. Let me rewrite it cleanly in one go:Ran a command, read a fileRan a command, read a fileThe carousel HTML is now just 5 lines — the script does all the work. To add or change photos, you only ever touch the config block at the top of the <script>: